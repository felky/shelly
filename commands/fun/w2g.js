module.exports = {
    name: 'w2g',
    cooldown: 5,
    description: 'Starts and links a new Watch2Gether page.',
    run: async(client, message, args) => {

        await message.channel.send(`This command is not finished yet, but please try again some other time!`);
    },
};